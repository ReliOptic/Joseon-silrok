# API server port (express + better-sqlite3)
API_PORT=3001

# SQLite database path (relative to repo root)
DB_PATH=db/sillok.sqlite

# APP_URL: The URL where this applet is hosted.
# AI Studio injects this at runtime with the Cloud Run service URL.
APP_URL="MY_APP_URL"
